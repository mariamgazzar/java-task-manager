# Java Task Manager (CLI)

A simple command-line task manager built in Java.

## Features
- Add tasks
- List tasks
- (Next) Mark tasks as done
- (Next) Save/load tasks from a file

## Project structure
- `src/Main.java` - main program

## How to run
Compile:
```bash
javac src/Main.java
